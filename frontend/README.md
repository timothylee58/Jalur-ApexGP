The Next.js app lives in `frontend/`. Set this project Root Directory to `frontend` in the Vercel dashboard.

Required env:
- NEXT_PUBLIC_API_URL — public FastAPI origin (Railway URL, no trailing slash)
